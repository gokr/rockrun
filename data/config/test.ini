; Rockrun test configuration overrides.
; Loaded after rockrun.ini with: rockrun -c test.ini --startup-test true
;
; VSync off: automated test environments (compositor throttling) can starve
; vsync'd present events and stall the renderer indefinitely.

[Display]
VSync           = false
